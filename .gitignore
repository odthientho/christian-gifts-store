# Dependencies
node_modules

# Build output
.next/
out/
dist/
.turbo/

# TypeScript
*.tsbuildinfo

# Env — never commit real secrets (each app/package keeps its own .env)
.env
.env.*
!.env.example

# OS / editor
.DS_Store

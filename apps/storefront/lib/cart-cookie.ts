/** Name of the httpOnly cookie holding the API cart's opaque token. */
export const CART_COOKIE = "cgs_cart";

/** Cart cookie lifetime. */
export const CART_COOKIE_MAX_AGE = 60 * 60 * 24 * 30; // 30 days
